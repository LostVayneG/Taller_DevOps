pipeline {
    agent any
    stages{
        stage('build'){
            steps{
                sh 'cp -r rp-portfolio /deploy'
                docker exec -it pythonserver echo "I'm inside the container!"
            }
        }
    }
}
