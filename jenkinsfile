pipeline {
    agent any
    stages{
        stage('build'){
            steps{
                sh 'cp -r rp-portfolio /deploy'
                sh 'docker exec -it jenkins echo "I'm inside the container!"'
            }
        }
    }
}
