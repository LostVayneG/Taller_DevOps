pipeline {
    agent any
    stages{
        stage('build'){
            steps{
                sh 'cp -rf ./rp-portfolio /deploy/'
                sh 'cd deploy'
                sh 'cd rp-portfolio'
                sh 'ls -l'
                sh 'python manage.py makemigrations projects'
                sh 'python manage.pymanage.py migrate projects'
            }
        }
        stage('deploy'){
            steps{
                sh 'cd /deploy/rp-portfolio/'
                sh 'python manage.py runserver'
            }
        }
    }
}
