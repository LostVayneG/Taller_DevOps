pipeline {
    agent any
    stages{
        stage('build'){
            steps{
                sh 'cp -r rp-portfolio /deploy'
                sh 'docker exec -it pythonserver echo "I'm inside the container!"'
            }
        }
    }
}
