pipeline {
    agent {
        docker{
            image 'python'
            args '--privileged'
        }
    }
    stages{
        stage('build'){
            steps{
                sh 'echo hola'
            }
        }
    }
}
